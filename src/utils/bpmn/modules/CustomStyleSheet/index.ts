import CustomStyleSheet, { StyleSheetOptions } from './CustomStyleSheet';

// 导出类和类型
export { CustomStyleSheet };
export type { StyleSheetOptions };

// 默认导出
export default CustomStyleSheet;

// 兼容性导出
export const customStyleSheet = CustomStyleSheet;
